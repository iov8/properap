"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
export function HomeSearch({ locations }: { locations: string[] }) { const [location, setLocation] = useState(""); const [intent, setIntent] = useState<"buy" | "rent" | "vacation">("buy"); const router = useRouter(); const submit = (event: React.FormEvent) => { event.preventDefault(); const params = new URLSearchParams(); if (location) params.set("location", location); params.set("intent", intent); router.push(`/properties?${params.toString()}`); }; return <form className="home-search" onSubmit={submit}><label><span>City or area</span><select value={location} onChange={(event) => setLocation(event.target.value)}><option value="">Any city or area</option>{locations.map((city) => <option key={city} value={city}>{city}</option>)}</select></label><div className="home-search-intents"><label><input type="checkbox" checked={intent === "buy"} onChange={() => setIntent("buy")} /> Buy</label><label><input type="checkbox" checked={intent === "rent"} onChange={() => setIntent("rent")} /> Rent</label><label><input type="checkbox" checked={intent === "vacation"} onChange={() => setIntent("vacation")} /> Vacation Rental</label></div><button type="submit">Search properties</button></form>; }
